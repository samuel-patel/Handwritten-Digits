In order to run the module in this repository you will need to have installed: tensorflow, numpy, pandas, and matplotlib.

Command to copy and paste:
```
python -m pip install tensorflow numpy pandas matplotlib
```
